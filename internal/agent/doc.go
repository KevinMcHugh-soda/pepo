// Package agent provides an extensible conversational agent that can call Pepo's HTTP API
// through structured tool invocations. The agent is designed to work with pluggable large
// language model backends such as OpenAI's GPT models.
package agent
